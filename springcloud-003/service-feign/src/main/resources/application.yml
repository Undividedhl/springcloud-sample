server:
  port: 9301

spring:
  application:
    name: service-feign
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8090/eureka

# springcloud-004
feign:
  hystrix:
    enabled: true
